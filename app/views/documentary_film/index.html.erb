<%= link_to 'Agregar Documental', documentary_film_new_path %>
<% @documentary_films.each do |df|%>
    <article>
  <header><%=df[:name]%></header>
  <%=df[:description]%>
  <footer>Director <%=df[:director]%> </footer>
</article>

<% end %>
