<%= link_to 'Agregar pelicula', movies_new_path %>
<% @movies.each do |movie|%>
    <article>
  <header><%=movie[:name]%></header>
  <%=movie[:description]%>
  <footer>Director <%=movie[:director]%> </footer>
</article>
<% end %>

